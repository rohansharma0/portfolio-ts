import { styled } from "styled-components";

export const StyledLogo = styled.div`
    a{
        text-transform: uppercase;
        line-height: 1;
        padding: 0.25rem 0;
        font-weight: 600;
    }
`